$(document).ready(function() {
  $("tr[id] , tbody[id]").hide();


  $("button[id]").click(function() {
    /* Act on the event */
    $("tr[id] , tbody[id]").show();

    $("tr[id] , tbody[id]").hide();

});

  $("button[id]").click(function() {
    /* Act on the event */
    $("tr[id] , tbody[id]").hide();
    $("tr[id] , tbody[id]").show();


  });
});
